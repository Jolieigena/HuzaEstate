import { RenovationProject } from "./types";

/** No real Renovate-studio backend exists yet — starts empty rather than
 *  fabricating demo projects. */
export function renovationSeedProjects(_ownerId: string): RenovationProject[] {
  return [];
}
