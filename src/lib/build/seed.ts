import { BuildProject } from "./types";

/** No real Build-studio backend exists yet — starts empty rather than
 *  fabricating demo projects. */
export function buildSeedProjects(_ownerId: string): BuildProject[] {
  return [];
}
